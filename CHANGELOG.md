# 更新说明 · 每天领赛博鸡蛋

版本号与 `SKILL.md` 的 `version` 字段一致，按时间倒序。

## 1.2.2 — 2026-09-22

依 SkillHub 平台评测报告整改。评测意见原文要点：文档「缺少最终输出样例（清单样式、HTML 效果），
FAQ 不够系统化，主要反模式说明分散在方法论章节中而非独立章节」。

- 新增「十、实际产出样例」：给出清单正文、单文件 HTML、变更报告三份交付物的真实形态，
  以及交付时的一句汇报口径。
- 新增「十一、反模式：应当避免的用法」独立章节，共 8 条（搜不到就编、重贴上期条目、
  用科普凑数、不写截止日期、不写信源级别、全量重读网站、替使用者判断资格、待确认与已核实混排）。
- 新增「十二、常见问题（FAQ）」独立章节，共 10 问：是否收费、耗时、是否重读全网、
  境外抓不到怎么办、单源失败会不会断、如何推送到手机、能否只盯自有目标、运行环境、
  产物可否接入其他流程、过期项如何处理。
- 原「十、配套文件」顺延为「十三、配套文件」。
- **同时补齐 `SKILL.md` 的 `version` 字段**：此前发布 1.2.1 时该字段未同步更新，仍停留在 1.2.0。
- **功能与脚本接口均无改动**。本次仅为文档整改。

## 1.2.1 — 2026-09-20

- 新增一键入口 `scripts/run_all.py`，把三条抓取命令串联执行，某趟失败不中断后续。
- 新增监控目标自定义：`watch_changes.py --init-user` 生成 `targets_user.json`，
  支持 `add` 加自有目标、`disable` 停用内置目标；不建该文件时行为与从前完全一致。
- 修正一处与脚本实际行为不符的文档表述。

## 1.2.0 — 2026-09-19

仓库初始提交版本。四类监控目标（文本模型额度／Agent 与 Coding 工具／视频与生图／认证福利），
四层信源体系，32 个内置监控目标，五条境外绕行通道，八类推送通道。纯 Python 标准库，零第三方依赖。

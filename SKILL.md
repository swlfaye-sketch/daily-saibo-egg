---
name: daily-saibo-egg
description: 每日清点国内外主流 AI 平台的免费额度——免费 token、Agent/Coding 工具赠送额度、视频与生图模型试用期、学生教师认证福利，汇总成一份「今天能薅什么、什么快到期」的清单。当用户要求「领赛博鸡蛋」「看看今天有什么免费的额度」「免费 token 汇总」「哪些模型免费」「AI 福利活动」，或需要维护这条产线时使用。
agent_created: true
---

# 每天领赛博鸡蛋

「一代人有一代人的鸡蛋要领。」——本 skill 每天清点一遍：国内外主流 AI 平台今天白送什么。

用户要的不是科普，是**可执行的清单**：哪些额度现在能领、怎么领、什么时候过期、过期前还剩几天。

**核心方法论**：「第一时间知道」靠的不是每天把几十个网站重读一遍——那既慢又必然漏。
靠的是**盯官方渠道的变更**＋**走绕行通道抓境外**＋**搜时效性活动**三条腿。

---

## 一、四类目标（缺一类就算没做完）

| 类别 | 具体内容 |
|---|---|
| **文本模型额度** | 永久免费模型、新用户赠送 token、限时免费模型 |
| **Agent / Coding 工具额度** | AI 编程工具、桌面 Agent 的赠送积分与限时折扣 |
| **视频 / 生图模型** | 每日免费生成次数、试用期、积分返还活动 |
| **认证类福利** | 学生认证、教师认证、开发者认证赠送的会员或积分 |

---

## 二、信源体系（四层，逐层递进）

**不要只用聚合网站。**聚合站信息滞后、会串错厂商、且只覆盖「永久免费层」。必须以官方渠道为主干。

### 第一层 · 官方直连（主干）

逐个平台盯官网与开放平台。**全平台名录与逐条实测状态见 `references/platforms.md`**（已探测 118 个目标），此处只讲分工原则：

- **国内平台**：实测 32/33 直达，是监控主力。但多数定价页是单页应用（返回 2—7KB 壳页），直抓取不到数字，**这类走官方文档与搜索**。
- **境外平台**：实测约半数在本机网络环境下不可达（Google 全系、Mistral、HuggingFace、xAI、Meta、Perplexity 等）。
  **但不要就此只靠搜索——实测已打通五条绕行通道：镜像站替代、官方 RSS 旁路、国内媒体搬运、免费额度专门站、搜索兜底。**
  逐条通道、逐平台对照方案见 **`references/overseas-bypass.md`**（本产线境外部分的主文件）。
- **优先盯「定价页」而不是首页**：免费政策调整必然先反映在定价页与额度说明页；首页只会告诉你发了什么新闻。

### 第二层 · 官方发布渠道（额度变化的真正首发地）

同一平台往往有多条发布渠道，**额度类消息常常不走官网首页**，须逐一覆盖：

| 渠道类型 | 说明 |
|---|---|
| **官方定价页 / 额度说明页** | 变化最灵敏，纳入每日快照比对 |
| **官方更新日志（Changelog / Release Notes）** | 模型与额度调整常注明在此 |
| **官方 News / Blog** | 活动与促销首发 |
| **官方开发者社区** | 国内平台（阿里云、腾讯云、火山引擎）的活动常只在此发布 |
| **官方 GitHub 仓库** | Release 与 CHANGELOG 反映官方工具的发版与政策变更 |
| **官方公众号** | 国内厂商活动的高频渠道 |
| **官方 X / Twitter** | 境外厂商首发渠道 |
| **官方小程序 / App** | **国内认证类活动常只在小程序内公示，网页查不到**，须留意 |
| **官方邮件订阅 / Discord** | 补充渠道 |

### 第三层 · 聚合与社区（只作线索）

- **GitHub 汇总仓库**：`mnfst/awesome-free-llm-apis`（甲级，见第三节）——raw **直连优先**（实测 6/6 可达），失败再轮换加速镜像
- **免费额度专门站**：`freellm.net`（**30 家 provider 逐家一页，含速率限制与是否需信用卡**，境外平台的最佳单点替代）、`freetokens.custats.info`（含核实状态与更新日期）
- **模型价格对比站**：`llm-price.com`（表格含**上架时间**，境外厂商上线与降价的灵敏哨点）、`llm-prices.com`、`aipricing.guru`、Artificial Analysis
- **国内科技媒体**：**境外厂商消息的第一时间中文搬运渠道**——量子位、IT之家、爱范儿、InfoQ 中国、开源中国、少数派（均有 RSS 可读）；新智元、智东西、雷峰网、CNBeta、新浪科技、网易科技、澎湃科技、虎嗅
- **技术社区**：掘金（抓包类文章是线索来源）
- **不可达**：Hacker News、Reddit r/LocalLLaMA、V2EX、知乎、极客公园在本机网络下不通，改走搜索

**用法纪律**：聚合站与社区**只作线索**。任何条目**回到官方页面核实后**才可写入清单；核不上的标注「待确认」，绝不写成既定事实。

### 第四层 · 变更哨兵（「第一时间」的关键机制）

**每天重读所有网页是行不通的。正确做法是抓一次、与昨日快照比对、只报变化。**

```bash
python scripts/watch_changes.py <数据目录>
```

**五类目标，各有专用指纹**：

| 类型 | 指纹算法 | 为什么这么设计 |
|---|---|---|
| `price` **定价页** | 只取「**价格数字集合 + 档位名集合**」 | 定价页普遍含 A/B 测试（实测 Cursor 定价页同一 URL 交替返回两个变体）。**全文指纹对定价页必然误报**；价格集合才反映真实政策 |
| `names` **表格型站点** | 只取**第一列名称集合** | 如 `llm-price.com` 每天新增行——全文指纹实测报「正文 4918→4897 字符」，**无法行动**；名称集合能直接说出「上架了哪几个模型」 |
| `page` 普通页面 | 全文去噪指纹 ＋ **相似度容差**（Jaccard ≥ 0.85 判为抖动） | 抹除构建 ID、nonce、时间戳；再吸掉轮播／随机推荐造成的假变化 |
| `rss` 订阅源 | 条目标识列表 | 只看有没有新条目，并直接列出新增标题 |
| `gh` 仓库 | **只取 `pushed_at`** | 星数每天微增，纳入指纹会天天误报 |
| `json` 接口 | **排序后**的条目标识 | 顺序变化（如热门榜重排）不算内容变化 |

首次运行建立基线，次日起即可比对。移出结构性噪声源后，**实测连续三轮零假变化、零失败**。

**两类噪声，任何指纹都救不了，只能移出监控：**

1. **站点对同一 URL 做 A/B 测试，且两个变体的结构本身不同**。实测 Cursor 定价页连抓 6 次——5 次返回 `$20/$60/$200`（档位含 Ultra），1 次返回 `$20/$40`（档位含 Team／Standard）。**这不是「指纹不准」，是页面本身不确定**，用阈值去救只会连带漏掉真变化。处置：移出哨兵，改由国内媒体与搜索覆盖，并在目标表写明移出理由。
2. **内容频繁变化、但变化与「免费额度」无关**。实测把 OpenAI Status（服务故障公告）放进监控后，**它一家占了 2/3 的报警量，而报警内容与额度毫无关系**——典型的「淹没真信号」。处置：移出，服务可用性交给该厂商的官方 RSS 覆盖。

**「单次失败」与「信源失效」必须分开处置**：脚本内置跨天失败计数，
单次失败只记「本次抓取失败」，**连续 3 次才升入「疑似失效」并在报告单列**。
理由：境外站点偶发超时是常态，若按单次失败就换源，会把好信源误判掉；
反之若无此机制，**一个真死掉的信源会永远躺在失败栏里无人察觉**。
抓取失败时**不覆盖旧快照**，故下期仍能正常比对。

报告会把「判为抖动」的目标单列，若某目标连续多日出现，说明该站改版较大，应改指纹方式。

**哨兵报「有变化」不等于「有新福利」**——可能是营销文案调整，必须打开页面确认。

---

## 三、每日流程（六步，顺序执行）

### 第一步 · 甲路：抓永久免费层硬数据

```bash
python scripts/fetch_tiers.py <数据目录>
```

两条信源：

1. **`mnfst/awesome-free-llm-apis`**（GitHub 汇总仓库）——实测：7,700+ 星，README 可 raw 直取，**16 家供应商、118 条模型条目**，含中国三家（智谱 Z AI、魔搭 ModelScope、硅基流动 SiliconFlow）。结构规整：供应商 → 免费说明 → Base URL → 模型表（上下文／最大输出／模态／限额）。该仓库**只收永久免费层**，明确排除限时促销，正好与乙路互补。
2. **OpenRouter 模型 API**（`https://openrouter.ai/api/v1/models`，免密钥）——判定免费：`pricing.prompt == 0` 且 `pricing.completion == 0`。实测全站 446—447 个模型（模型数在变）中 25 个零价，**比仓库更实时**，两源冲突时以此为准。

**已知失效信源**：`cheahjs/free-llm-api-resources` 已 404（该领域曾最知名的仓库）。`NAZCAMEDIA/free-llm-api-resources` 是他人 fork，最后更新 2026-02-18，仅作参考。若主仓库拉取失败，先到 GitHub 搜同类仓库换源，不要直接报「无数据」。

### 第二步 · 境外路：走绕行通道抓境外信源

```bash
python scripts/fetch_overseas.py <数据目录>
```

**境外官网约半数直连不通，但消息并非拿不到**——本脚本只走实测可达的七条通道，不硬试不通的域名：

| 通道 | 抓什么 |
|---|---|
| **freellm.net 详情页** | 18 家重点境外供应商的**速率限制、免费模型数、是否需信用卡、最近更新日** |
| **llm-price.com** | 模型价格表，含**上架时间**（"yesterday"／"5 days ago"）——上线与降价的灵敏哨点 |
| **hf-mirror.com** | 替代不通的 HuggingFace：趋势模型（含下载／点赞）＋每日论文 |
| **OpenRouter API** | 免密钥，实时判定输入输出双零价模型 |
| **GitHub 汇总仓库** | 取回 `mnfst` 仓库 README。**直连 `raw.githubusercontent.com` 优先**（实测 6/6 可达），失败再轮换加速镜像 |
| **官方 RSS 直连** | OpenAI News、DeepMind Blog、GitHub Blog（官网 403 或不通，RSS 通） |
| **国内媒体 RSS** | 量子位／IT之家／爱范儿／InfoQ／开源中国／少数派，按关键词筛「免费／赠送／额度／降价」 |

**实测一次跑通的产出**：30 家 provider 清单、18 家详情、24 行价格动态、20 个趋势模型、
15 篇论文、25 个零价模型、汇总仓库全文、3 条官方 RSS、6 条媒体流。

**通道细节与逐平台对照方案见 `references/overseas-bypass.md`。**

### 第三步 · 哨兵：跑变更检测

```bash
python scripts/watch_changes.py <数据目录>
```

产出「变更报告」。**只把真正变化的目标送进后续步骤核实**，不必逐个重读。

### 第四步 · 乙路：搜索时效性活动

甲路与哨兵解决不了「刚上线的活动」，必须搜。逐条搜以下关键词：

```
1. 免费 token 赠送 活动 AI 大模型 新用户 额度          ← 时效过滤：近 7 天
2. AI 工具福利 免费会员 限时 折扣 汇总                ← 时效过滤：近 7 天
3. AI 编程工具 免费额度 赠送 积分 Trae Qoder Cursor
4. 免费 视频生成 生图模型 每日免费 试用期
5. 学生认证 教师认证 免费领取 AI 会员 积分
6. 新模型 限时免费 试用 大模型 发布
7. （按需补充当前热点产品名，如各家新出的 Coding 工具、Agent 产品）
```

**注意**：视频与生图类平台的免费额度变动最频繁，且常只在小程序或 App 内公示，**不要因为官网没写就断定没有**。

### 第五步 · 汇总成清单

按第四节格式输出 Markdown，写清信源级别与核实状态。

### 第六步 · 渲染 HTML（手机可读的那一份）

```bash
python scripts/render_html.py <清单.md> <输出目录>
```

产出与清单同名的 `.html`，单文件、无外部依赖，**双击即可打开，手机上也读得顺**。

**为什么要交给脚本而不是每次手写 HTML**：版式稳定（每天长得一样，一眼认得出）、省积分（渲染是确定性工作，模型只管内容）、不出错（表头与转义由脚本负责，不会写坏标签）。

**渲染器自带的版式规则**：按标题关键词自动给分栏上色与配图标——「到期」红色加🔥（**最醒目，使用者最怕错过截止日期**）、「无条件／立即」绿色加🎁、「认证」琥珀色加🪪、「永久免费」蓝色加♾️、「无变化」灰、「免责」浅灰。表格在窄屏可横向滑动。

**每条清单交付时给两份**：Markdown 便于复制与再编辑，HTML 便于直接阅读。

---

## 三之附 · 境外不通怎么办（核心机制）

**这是本产线最关键的一节：境外平台不可达，不等于拿不到它的消息。**

五条通道按可靠性排序，**不得只靠搜索**：

| 通道 | 覆盖 | 实测 |
|---|---|---|
| **镜像站替代** | HuggingFace → `hf-mirror.com`（全套：模型 API／博客／论文／趋势／定价）；GitHub raw → `ghproxy.net` 等**多个加速镜像轮换**；OpenAI 文档 → `openai.wiki` | 取回字节数与源站**完全一致**（28,367 B），内容未篡改 |
| **官方 RSS 旁路** | OpenAI News（720 KB）、DeepMind Blog（69.8 KB）、GitHub Blog（724 KB）、OpenAI Status（802 KB）、Anthropic News（451 KB）、Stability News（314 KB） | **官网 403／不通，RSS 照通** |
| **国内媒体搬运** | 量子位、IT之家、爱范儿、InfoQ、开源中国、少数派（RSS 可读）＋ 新智元、智东西、雷峰网、CNBeta、新浪／网易／澎湃科技、虎嗅 | 17/17 主站可达——**时效性最好的一路** |
| **免费额度／价格专门站** | `freellm.net`（30 家 provider 逐家一页）、`llm-price.com`（含上架时间）、`freetokens.custats.info`、Artificial Analysis | **境外主体但本机全部可直连** |
| **搜索兜底** | **全部境外平台**，含本机完全不通的 `ai.google.dev` | 实测取回 Google 官方定价页**完整中文口径** |

**已确认不通、不必浪费时间的域名**（换路径也没用）：
`blog.google` 全系、`ai.google.dev`、`cloudblog.withgoogle.com`、`ai.meta.com`、
`mistral.ai`、`docs.mistral.ai`、`x.ai`、`huggingface.co`、`perplexity.ai`、
`lumalabs.ai`、`pika.art`、`lmarena.ai`；
`midjourney.com`、`platform.openai.com` 返 403。

> **这张名单是「多次复测仍全败」的结果，不是一两次探测的结论。**
> 凡单次超时、`IncompleteRead`、或页面大于 0.5 MB 的失败，**都不足以判为不可达**——
> 实测 `github.com` 系列（1.26 MB 的方案页）与 `raw.githubusercontent.com` 都曾被误判为不可达，
> 放宽超时后全部复通。**判据与排除办法见第九节第一条。**

**RSSHub 公共实例不要高估**：实测 29 条常用路由**仅 4 条可用**
（`/openai/news`、`/huggingface/daily-papers`、`/hackernews/best`、`/ithome/it`），
其余大量 503。可用实例：`rsshub.rssforever.com`、`rsshub.liumingye.cn`、`rsshub.woodland.cafe`。
**「RSSHub 能把任意网站变 RSS」是误解**，只作补充，不列主通道。

---

## 四、每条信息的必查四项

搜到一条福利，**四项填不齐就不写进清单**：

1. **额度**——具体是多少（3 万 token／1000 积分／每月 100 万），不许写「大量」「丰厚」。
2. **领取条件**——要不要实名、绑卡、学生或教师认证、是否限新用户。
3. **截止日期**——写年月日；**查不到就写「未标明」，绝不推算、绝不编造**。
4. **信源链接**——官方页面优先，并标注信源级别。

### 信源分级

| 级别 | 类型 | 用法 |
|---|---|---|
| **甲级** | 厂商官方活动页、官方文档、官方公告、可程序化核实的接口数据 | 直接采信 |
| **乙级** | 官方开发者社区、官方公众号发文 | 采信，标注来源 |
| **丙级** | 第三方「福利大全」聚合文章 | **只作线索**，回官方核实后才写入；核不上标「待确认」 |

丙级文章覆盖面广（一次可列二十余家厂商），但会过时、会串行错位（把 A 厂商的活动写到 B 厂商名下）。**绝不能直接转述。**

---

## 五、输出格式

### 按「紧迫度」分组，不按厂商分组

使用者最怕的是**错过截止日期**，所以排序逻辑是过期时间，不是厂商名气：

1. **今天就到期**（红色警示，放最前）
2. **本周内到期**（按日期升序）
3. **本月内到期**
4. **长期有效／永久免费**（压轴，不用着急）

### 每条一行，信息密度拉满

```
某 Coding 工具夜间额度加成
额度：23:00—9:00 期间指定模型额度归零、其他 Agent 翻倍
条件：注册即用，无需认证
截止：2026年9月20日（还剩 1 天）
信源：<官方链接>（甲级）
```

### 另设三个专列

- **新出现的**——相比上一期新冒出来的活动，避免使用者重复看旧信息
- **今天到期／已过期**——明确标记，过期项不再出现在可领取区
- **本期无新增**——搜不到就如实写，不许为凑数重复上期条目

### 收尾必带免责声明

注明：信息来自各厂商公开渠道，活动规则与截止时间可能随时调整，**参与前以官方页面为准**；标注「待确认」的条目尚未回溯官方，不具备决策效力；有身份认证要求的活动须自行确认资格。

---

## 六、资格门槛（写清条件，不替使用者下结论）

清单是给使用者本人看的，不是把全网活动罗列一遍。凡有身份门槛的福利，**必须写清门槛，由使用者自行判断是否具备资格**：

| 门槛类型 | 处理方式 |
|---|---|
| 学生认证 | 写清「限何类院校、何种在读状态」及认证渠道 |
| 教师认证 | 写清「限何类学校在职教师」及认证渠道（国内多为教育部「中国教师」小程序核验） |
| 开发者 / 企业认证 | 写明所需材料 |
| 需境外手机号 / 境外支付方式 | 如实标注，不代为判断 |
| 需绑定信用卡 | 标注「需绑卡」，把决定权留给使用者 |
| 需实名认证 | 注明，并说明是否影响免费额度领取 |

**纪律**：拿不准使用者是否符合资格时，**标注「需自行确认资格」，不要替他下结论说能领**；但若门槛明显是身份性质不符（例如要求在校教师而使用者是企业在职人员），应明确讲清卡点在哪，省得他白跑一趟。

---

## 七、交付

**每条清单出两份**（Markdown 便于复制编辑，HTML 便于直接阅读）：

- 按日归档：`out/赛博鸡蛋-YYYY-MM-DD.md` ＋ 同名 `.html`
- 固定入口：`out/最新一期.md` 与 `out/最新一期.html`（每次覆盖，点开即看当天）
- 变更报告：`out/变更报告_YYYY-MM-DD.md`
- 交付后一句话报三个数：**今天能领几项、几项快到期、有没有新出现的**

**若挂每日自动化**：跑完必须把两份文件拷到使用者容易找到的位置（如桌面固定目录），
**不要只落在项目目录里**——使用者不会去翻项目文件夹。
另注意机器需处于开机状态，否则该次不执行。

---

## 八、红线

1. **不许编造额度与日期**。查不到截止日期就写「未标明」；额度不确定就不写。
2. **不许把「可能」「听说」写成既定事实**。丙级信源未核实的内容，明确标注「待确认」。
3. **不许写需要绕过风控或违反平台条款的「薅法」**。只写官方渠道的正当领取方式。
4. **数量不作目标**。宁可写 5 条准确的，不写 20 条含水的。
5. **哨兵报变化不等于报福利**，必须人工确认后才可写入清单。
6. **国内政府网站、UGC 平台不作信源**。

---

## 九、已知坑（实战踩过）

- **⚠️ 最严重的一类错：把「大页面超时」误判成「域名不可达」。** 本产线犯过两次，两次都把可用通道封死。判定「不可达」前必须排除三种假象：

  | 假象 | 实测案例 | 排除办法 |
  |---|---|---|
  | **大页面在紧超时内传不完** | `github.com/features/copilot/plans` 是 **1.26 MB**，用 25 s 超时**连续 5 次失败**，被误判为「github.com 被墙」；放宽到 45–75 s 后 **5/6 成功** | **页面越大超时越要放宽**：0.5 MB 给 45 s，1 MB 以上给 75 s |
  | **传输中断 `IncompleteRead`** | 495 KB 的仓库页 6 次中 1 次中断 | 属传输未完成，**与超时同类，一并重试**，不是封锁 |
  | **单次抖动窗口** | `raw.githubusercontent.com` 初测超时被记为「直连不通」，复测 **6/6 稳定可达** | **至少 5–6 次复测**；单次结果一律不定论 |

  **唯一可信的判据**：同一目标**多次复测仍全败**，且换路径（RSS／API／镜像）同样全败。**只测一两次就写进「不可达」名单，比漏掉一个信源更糟——它会把好通道永久封死。**
- **GitHub 仓库会改名或消失**：`cheahjs/free-llm-api-resources` 曾是该领域最知名仓库，现已 404。甲路仓库拉取失败时先换源，不要直接报「无数据」。
- **GitHub raw 与 API 均可直连（初版结论已推翻）**：`raw.githubusercontent.com` 实测 **6/6 稳定可达**（28,367 B，7.9 s），`api.github.com` 6/6，`github.com` 首页 6/6（575 KB）、`/trending` 6/6（646 KB）。**直连优先，加速镜像（`ghproxy.net`／`gh-proxy.com`／`gh.xxooo.cf`／jsDelivr）只作冗余**；`gh.llkk.cc`／`ghp.ci`／`github.moeyy.xyz`／`hub.gitmirror.com`／`raw.gitmirror.com`／`raw.kkgithub.com` 全不通。
- **hf-mirror 有独立限流**：`/papers` 复测出现 429。镜像站限流严于源站，**一天一次足够，不要连抓**。
- **freellm.net 并发过高会被整体限流**：实测 6 并发抓 18 个详情页时，**连首页都返回超时（HTTP -1）**。已降到 4 并发并加 3 次重试。**境外专门站都要按这个标准设防。**
- **freellm 详情页的模型 ID 会混入 Astro CSS 类名**：页面里满是 `provider-shell`、`provider-hero-card`、`provider-logo-box` 等，且满足「带连字符的小写串」正则，**必须显式过滤 `provider-` 前缀**，否则结果全是噪声。
- **速率限制数字带千分位逗号**：`1,000 RPD` 用 `(\d+)` 会截成 `000`，须用 `([\d,]{1,7})`。
- **定价页普遍含 A/B 测试，全文指纹必然误报**：实测 Cursor 定价页**同一 URL 交替返回两个变体**（正文 7474／8086 字符），全量 5-gram 相似度稳定在 **0.7986**。
  **结论：定价页一律用 `kind="price"` 结构化指纹**（只取「价格数字集合 + 档位名集合」）。对照实测：Anthropic 定价页连抓 3 次完全相同（20705 字符、27 个价格项），证明该指纹对稳定站点有效。
- **但 A/B 变体套餐结构也不同的页面，指纹无解，须移出监控**：Cursor 定价页连抓 6 次——5 次给 `$20/$60/$200`（档位含 Ultra），1 次给 `$20/$40`（档位含 Team／Standard）。**两套套餐结构不同，任何指纹都会交替报警。**
  处置是**移出哨兵**（已在 `watch_changes.py` 目标表中注明理由），改由国内媒体与搜索覆盖。**不要靠调阈值硬扛——那会连带漏掉真变化。**
- **星数不要进指纹**：GitHub 仓库星数每天微增，纳入指纹会天天误报。仓库元数据**只取 `pushed_at`**。
- **相似度阈值不能拍脑袋**：MinHash 采样 80 个样本时，对同一内容的 Jaccard 估计误差约 ±11%（实测真值 0.7986，N=80 估出 0.7391）。样本太少既会漏判也会误判，**要实测真值再定阈值**。
- **Bing 结果页可达但不可解析**：`cn.bing.com/search` 返回 96 KB，按 `<h2><a>` 结构解析出 **0 条**（结构已变或 JS 渲染）。**程序化搜索不要走 Bing 抓取**，直接用工具的搜索能力。
- **RSSHub 公共实例覆盖率很低**：实测 29 条常用路由**仅 4 条可用**，其余大量 503。「RSSHub 能把任意网站变 RSS」是误解，只作补充。
- **国内平台定价页多为单页应用**：智谱 `open.bigmodel.cn/pricing` 直抓仅返回约 3.9KB 壳页，取不到内容。国内额度信息**走搜索与官方文档，不走页面直抓**。
- **境外平台半数不可达**：Google 全系、Mistral、HuggingFace、xAI、Meta、Perplexity、Luma、Pika 直连失败；Groq Console、Cerebras、Together、Midjourney、Lovable 返回 403（Cloudflare 反爬）。**不要硬试换路径——换路径无效，直接走 `references/overseas-bypass.md` 的五条通道。**
- **官方 RSS 也会偶发失败**：同一轮实测中三条官方 RSS 一次 3/3 成功、一次 2/3。**属网络抖动，单次结果不能定论，须复测。**
- **国内平台账号接口需密钥**：硅基流动 `/v1/models`、智谱 `/api/paas/v4/models` 均返回 401，属正常，不要误判为信源失效。
- **正则与编码**：脚本一律用文件写入方式编写，**不要用 heredoc**（会吃掉 `\`，导致正则报 `unterminated character set`）；Windows 环境下 Git Bash 的 `/tmp` 与原生工具的 `/tmp` 不是同一位置，临时文件落盘到工作目录。
- **Nuxt / Vue 站点载荷的键名不带引号**：形如 `mark_content:e`（值为变量引用），用 `"key":"value"` 的正则必然匹配不到，须用 `"?key"?\s*:`。
- **同一文件的多处编辑必须串行**：并行下发两次编辑，先写的会被覆盖（返回成功但未落盘）。改完须复核。

---

## 十、配套文件

| 文件 | 用途 |
|---|---|
| `scripts/fetch_tiers.py` | 甲路：抓永久免费层（汇总仓库 + OpenRouter），产出 JSON 与 Markdown |
| `scripts/fetch_overseas.py` | **境外路：走七条绕行通道抓境外信源**（镜像站／官方 RSS／国内媒体／专门站／加速镜像） |
| `scripts/watch_changes.py` | 哨兵：官方渠道变更检测（`price` 定价页／`names` 表格站／`page` 网页／`rss` 订阅源／`gh` 仓库／`json` 接口）＋ 跨天失败计数（连续 3 次失败升为「疑似失效」） |
| `scripts/render_html.py` | 清单渲染为**单文件 HTML**（手机可读、按紧迫度自动配色配图标），供交付时出第二份 |
| `references/overseas-bypass.md` | **境外绕行通道专章**：五条通道实测、逐平台对照方案、已确认不通的域名清单 |
| `references/platforms.md` | **全平台信源名录**：118 个目标的逐条实测状态、官方渠道、绕行方案 |
| `references/sources.md` | 信源实测记录与失效清单 |
